# OS/editor junk
*.swp
*.swo
*~
.DS_Store

# secrets — never commit these
*.pem
*.key
id_*
.ssh/

# caches
.cache/
__pycache__/
*.pyc

# stow internal
.stow-local-ignore

# large stuff that shouldn't be in git
*.zip
*.tar.*
node_modules/

# wallpapers — synced from server via sync-wallpapers.sh, not in git
wallpapers/

# private folder — its own private git repo on GitHub (dotfiles-private)
private/

# machine-specific monitor config (each host owns this file locally)
hypr/.config/hypr/monitors.conf
hypr/.config/hypr/workspaces.conf

# tmux plugins — installed at runtime by TPM (prefix + I)
tmux/.config/tmux/plugins/

# backup files
*.bak

# font cache (auto-generated by fontconfig)
fontconfig/.config/fontconfig/*.cache*

# opencode — plugin deps are generated at runtime; API keys come from env (private/local.zsh)
opencode/.config/opencode/package.json
opencode/.config/opencode/package-lock.json
opencode/.config/opencode/bun.lock
